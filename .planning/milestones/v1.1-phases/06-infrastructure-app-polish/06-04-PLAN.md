---
phase: 06-infrastructure-app-polish
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/generate-app-icon.swift
autonomous: false
requirements: [INFRA-02]
gap_closure: true

must_haves:
  truths:
    - "App icon bars match brand kit SVG coordinates exactly (no 1.2x scale, no forced bottom-alignment)"
    - "Dark mode icon has distinct gradient (#1C2333 -> #111827) and brighter bar colors"
    - "Tinted icon uses correct brand kit geometry"
  artifacts:
    - path: "scripts/generate-app-icon.swift"
      provides: "Corrected icon generation with exact brand kit proportions"
      contains: "scaleFactor"
    - path: "DictusApp/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png"
      provides: "Standard app icon with correct bar positions"
    - path: "DictusApp/Assets.xcassets/AppIcon.appiconset/AppIcon-1024-dark.png"
      provides: "Distinct dark variant with surface gradient"
  key_links:
    - from: "scripts/generate-app-icon.swift"
      to: "assets/brand/dictus-brand-kit.html"
      via: "SVG viewBox coordinates scaled to 1024px"
      pattern: "scaleFactor.*1024.*80"
---

<objective>
Fix app icon generation to match brand kit proportions exactly.

Purpose: UAT test 1 failed — bars have wrong proportions (1.2x too large), wrong positioning (forced bottom-alignment instead of brand kit staggered positions), and no dark/light differentiation.
Output: Corrected generate-app-icon.swift + regenerated PNGs with proper light/dark/tinted variants.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-app-polish/06-UAT.md
@.planning/debug/app-icon-proportions.md
@assets/brand/dictus-brand-kit.html

<interfaces>
<!-- Brand kit SVG bar geometry (80x80 viewBox) — the source of truth -->

Bar 1 (left):   x=19, y=34, width=9, height=18, rx=4.5, fill=white opacity=0.45
Bar 2 (center): x=35.5, y=22, width=9, height=42, rx=4.5, fill=gradient #6BA3FF -> #2563EB
Bar 3 (right):  x=52, y=29, width=9, height=27, rx=4.5, fill=white opacity=0.65

Background gradient (standard/light): #0D2040 -> #071020 at 135 degrees
Background gradient (dark/surface):   #1C2333 -> #111827 at 135 degrees
Stroke glow: rgba(61,126,255,0.5) -> rgba(61,126,255,0) at 135 degrees

Bar bottom edges are STAGGERED (not aligned):
  Bar 1 bottom: 34 + 18 = 52
  Bar 2 bottom: 22 + 42 = 64
  Bar 3 bottom: 29 + 27 = 56

Scale factor: 1024 / 80 = 12.8 (apply to ALL coordinates and dimensions, NO additional multiplier)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix bar geometry and positioning in generate-app-icon.swift</name>
  <files>scripts/generate-app-icon.swift</files>
  <action>
Rewrite the bar geometry section of scripts/generate-app-icon.swift to use exact brand kit coordinates:

1. REMOVE the 1.2x multiplier from all bar dimensions (lines 47-58). Use pure scaleFactor (1024/80 = 12.8) only.

2. REPLACE the bottom-alignment positioning logic (lines 67-83) with absolute positioning from brand kit SVG coordinates. Each bar has its own x and y position from the SVG, scaled by 12.8:
   - Bar 1: x = 19 * 12.8 = 243.2, y = 34 * 12.8 = 435.2, w = 9 * 12.8 = 115.2, h = 18 * 12.8 = 230.4, rx = 4.5 * 12.8 = 57.6
   - Bar 2: x = 35.5 * 12.8 = 454.4, y = 22 * 12.8 = 281.6, w = 9 * 12.8 = 115.2, h = 42 * 12.8 = 537.6, rx = 4.5 * 12.8 = 57.6
   - Bar 3: x = 52 * 12.8 = 665.6, y = 29 * 12.8 = 371.2, w = 9 * 12.8 = 115.2, h = 27 * 12.8 = 345.6, rx = 4.5 * 12.8 = 57.6

   Store these as simple arrays: barXPositions, barYPositions, barWidths (all same), barHeights. No centering logic, no bottom-alignment logic — just scaled coordinates.

3. IMPLEMENT distinct generateDark() function (currently a pass-through to generateStandard on lines 193-195):
   - Background gradient: #1C2333 -> #111827 at 135 degrees (the "surface" variant from brand kit)
   - Center bar gradient: #93C5FD -> #3B82F6 (brighter blue, from the brand kit "Sur fond surface" variant)
   - Side bar opacities: 0.35 (left) and 0.55 (right) — slightly dimmer than standard, matching brand kit surface variant
   - No stroke glow on dark variant (cleaner look)

4. Fix the cornerRadius in drawBars() — currently uses the same 1.2x-scaled value. Use 57.6 (4.5 * 12.8).

5. Update drawBars() to accept bar opacity overrides so generateDark can pass different opacities. Add parameter: `barOpacityOverrides: [CGFloat]? = nil` and use it when provided, falling back to the default [0.45, 1.0, 0.65].

6. Update drawBackground() to accept custom gradient colors as parameters so generateDark can pass its own gradient. Add parameters: `gradientStart: CGColor = bgStart, gradientEnd: CGColor = bgEnd`.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && swift scripts/generate-app-icon.swift && ls -la DictusApp/Assets.xcassets/AppIcon.appiconset/*.png && python3 -c "
import hashlib, sys
files = ['DictusApp/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png', 'DictusApp/Assets.xcassets/AppIcon.appiconset/AppIcon-1024-dark.png']
hashes = [hashlib.md5(open(f,'rb').read()).hexdigest() for f in files]
if hashes[0] == hashes[1]:
    print('FAIL: standard and dark icons are identical')
    sys.exit(1)
else:
    print('PASS: standard and dark icons are distinct')
"</automated>
  </verify>
  <done>
    - Script runs without errors and generates 3 PNGs (standard, dark, tinted)
    - Standard and dark PNGs are NOT byte-identical (md5 hashes differ)
    - Bar coordinates use exact brand kit values scaled by 12.8 with no additional multiplier
    - No forced bottom-alignment in positioning code
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify app icon matches brand kit</name>
  <files>DictusApp/Assets.xcassets/AppIcon.appiconset/AppIcon-1024.png</files>
  <action>
User visually verifies the generated app icon PNGs match the brand kit proportions.
What was built: Regenerated app icon PNGs with corrected brand kit proportions and distinct dark variant.
Steps:
1. Open Xcode project and navigate to DictusApp/Assets.xcassets/AppIcon.appiconset/
2. Compare the 3 PNGs: standard (dark navy bg), dark (surface gradient), tinted (black on transparent)
3. Build and run on device — check home screen icon in light mode and dark mode
4. Compare with assets/brand/dictus-brand-kit.html — bar proportions should match "Sur fond sombre"
  </action>
  <verify>User confirms icon matches brand kit visually</verify>
  <done>User approves icon appearance or describes remaining issues</done>
</task>

</tasks>

<verification>
- `swift scripts/generate-app-icon.swift` runs without errors
- 3 distinct PNG files exist in AppIcon.appiconset/
- Standard and dark PNGs have different file hashes
</verification>

<success_criteria>
App icon on home screen matches brand kit proportions: bars at staggered positions (not bottom-aligned), correct sizes (no 1.2x bloat), and visually distinct light/dark variants.
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-app-polish/06-04-SUMMARY.md`
</output>
