---
phase: 04-main-app-onboarding-and-polish
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - DictusApp/Views/RecordingView.swift
  - DictusApp/Views/HomeView.swift
  - DictusApp/Views/MainTabView.swift
  - DictusApp/Views/ModelManagerView.swift
  - DictusKeyboard/Views/RecordingOverlay.swift
  - DictusKeyboard/Views/ToolbarView.swift
  - DictusKeyboard/Views/KeyboardView.swift
  - DictusKeyboard/Views/KeyButton.swift
  - DictusKeyboard/Views/FullAccessBanner.swift
  - DictusKeyboard/KeyboardRootView.swift
autonomous: false
requirements:
  - KBD-06
  - DSN-01
  - DSN-02
  - DSN-03
  - DSN-04

must_haves:
  truths:
    - "Every surface in the app uses glass (iOS 26) or Material (iOS 16-25)"
    - "RecordingView in main app uses 3-bar BrandWaveform instead of 50-bar WaveformView"
    - "RecordingOverlay in keyboard uses 3-bar BrandWaveform instead of 30-bar KeyboardWaveformView"
    - "ToolbarView mic button uses AnimatedMicButton with glow/pulse/shimmer states"
    - "Keyboard keys and toolbar have glass styling on iOS 26"
    - "All text uses Dynamic Type system font styles — no fixed font sizes"
    - "Light and dark mode render correctly on every screen"
  artifacts:
    - path: "DictusApp/Views/RecordingView.swift"
      provides: "Redesigned recording UI with BrandWaveform"
      contains: "BrandWaveform"
    - path: "DictusKeyboard/Views/RecordingOverlay.swift"
      provides: "Redesigned keyboard recording with BrandWaveform"
      contains: "BrandWaveform"
    - path: "DictusKeyboard/Views/ToolbarView.swift"
      provides: "Toolbar with AnimatedMicButton"
      contains: "AnimatedMicButton"
    - path: "DictusKeyboard/Views/KeyButton.swift"
      provides: "Glass-styled key buttons"
      contains: "dictusGlass"
  key_links:
    - from: "DictusApp/Views/RecordingView.swift"
      to: "DictusApp/Design/BrandWaveform.swift"
      via: "import and use BrandWaveform(energy:)"
      pattern: "BrandWaveform"
    - from: "DictusKeyboard/Views/ToolbarView.swift"
      to: "AnimatedMicButton component"
      via: "Replaces inline micIcon with AnimatedMicButton"
      pattern: "AnimatedMicButton"
    - from: "DictusKeyboard/Views/KeyButton.swift"
      to: "GlassModifier"
      via: ".dictusGlass() on key backgrounds"
      pattern: "dictusGlass"
---

<objective>
Apply the design system across all existing screens — glass styling, brand waveform replacement, mic button animations, Dynamic Type, and light/dark mode audit.

Purpose: Transform every screen in the app and keyboard extension to use the iOS 26 Liquid Glass design language (with Material fallback), replace generic waveforms with the logo-inspired 3-bar BrandWaveform, and wire AnimatedMicButton into the keyboard toolbar. This is the visual polish pass that makes Dictus look like it belongs on iOS 26.

Output: Updated RecordingView, RecordingOverlay, ToolbarView, KeyButton, KeyboardView, FullAccessBanner, HomeView, ModelManagerView with glass + brand waveform + typography.
</objective>

<execution_context>
@/Users/pierreviviere/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierreviviere/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-main-app-onboarding-and-polish/04-CONTEXT.md
@.planning/phases/04-main-app-onboarding-and-polish/04-RESEARCH.md
@.planning/phases/04-main-app-onboarding-and-polish/04-01-SUMMARY.md

@DictusApp/Views/RecordingView.swift
@DictusApp/Views/HomeView.swift
@DictusApp/Views/ModelManagerView.swift
@DictusKeyboard/Views/RecordingOverlay.swift
@DictusKeyboard/Views/ToolbarView.swift
@DictusKeyboard/Views/KeyboardView.swift
@DictusKeyboard/Views/KeyButton.swift
@DictusKeyboard/Views/FullAccessBanner.swift
@DictusKeyboard/KeyboardRootView.swift
@DictusApp/Design/GlassModifier.swift
@DictusApp/Design/BrandWaveform.swift
@DictusApp/Design/AnimatedMicButton.swift
@DictusApp/Design/DictusColors.swift
@DictusApp/Design/DictusTypography.swift

<interfaces>
<!-- Design components from Plan 04-01 -->

From DictusApp/Design/GlassModifier.swift:
```swift
extension View {
    func dictusGlass(in shape: some Shape = RoundedRectangle(cornerRadius: 16)) -> some View
    func dictusGlassBar() -> some View  // for toolbars
}
```

From DictusApp/Design/BrandWaveform.swift:
```swift
struct BrandWaveform: View {
    let energy: Float      // 0.0 - 1.0
    var maxHeight: CGFloat  // default 80
}
```

From DictusApp/Design/AnimatedMicButton.swift:
```swift
struct AnimatedMicButton: View {
    let status: DictationStatus
    let onTap: () -> Void
}
```

From DictusApp/Design/DictusTypography.swift:
```swift
extension Font {
    static let dictusHeading: Font      // .rounded .bold
    static let dictusSubheading: Font   // .rounded .semibold
    static let dictusBody: Font         // .body
    static let dictusCaption: Font      // .caption
}
```

From DictusApp/Design/DictusColors.swift:
```swift
extension Color {
    static let dictusBackground, dictusAccent, dictusAccentHighlight: Color
    static let dictusSurface, dictusRecording, dictusSuccess: Color
    init(hex: String)
}
```

<!-- Keyboard extension cannot import DictusApp, so GlassModifier/BrandWaveform/AnimatedMicButton
     must be DUPLICATED or shared differently. Options:
     1. Copy the files into DictusKeyboard target (simplest, some duplication)
     2. Move them into DictusCore SPM package (cleanest, but adds UIKit/SwiftUI dep to shared package)
     Option 1 is recommended: copy GlassModifier, BrandWaveform, AnimatedMicButton, DictusColors
     into DictusKeyboard. The code is small (~200 lines total). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Brand waveform + glass on main app screens</name>
  <files>
    DictusApp/Views/RecordingView.swift,
    DictusApp/Views/HomeView.swift,
    DictusApp/Views/ModelManagerView.swift,
    DictusApp/Views/MainTabView.swift
  </files>
  <action>
    **RecordingView.swift — Full redesign:**
    - Replace the 50-bar WaveformView with BrandWaveform component
    - Recording state: BrandWaveform(energy: coordinator.currentEnergy) where currentEnergy is a Float derived from the most recent bufferEnergy value. If `coordinator.bufferEnergy` is `[Float]`, compute the average or max of the last few values to get a single 0.0-1.0 float. Use `.maxHeight(120)` for the larger main app view.
    - Remove the old `WaveformView` struct entirely (it's only used here)
    - Keep the dark background (Color.dictusBackground instead of Color.black)
    - Apply `.dictusHeading` to title text, `.dictusBody` to body text
    - Timer: use `.font(.system(size: 48, weight: .light, design: .monospaced))` — keep monospaced for timer readability
    - Stop button: replace the generic stop.circle.fill with a styled button using accent color
    - Ready state: use Color.dictusSuccess for the checkmark
    - Failed state: use branded colors
    - Apply `.dictusGlass()` to the transcription result card in ready state
    - Ensure all colors use DictusColors — no hardcoded Color(.red), Color.black, etc.

    **HomeView.swift — Glass + typography pass:**
    - Apply `.dictusGlass()` to card surfaces (last transcription card, model status card, "download a model" prompt card)
    - Use `.dictusHeading` for "Dictus" title, `.dictusSubheading` for section headers
    - Use `.dictusBody` for descriptions
    - Ensure all card text and icons use semantic colors (foregroundStyle(.primary), .secondary) for automatic light/dark support
    - "Tester la dictee" button: use Color.dictusAccent background with white text

    **ModelManagerView.swift — Glass + typography pass:**
    - Apply `.dictusGlass()` to model cards/rows
    - Use `.dictusHeading`/`.dictusSubheading` for model names
    - Download progress: tint with Color.dictusAccent
    - Active model badge: Color.dictusSuccess background
    - Ensure no hardcoded colors remain — review every `Color(...)` usage

    **MainTabView.swift — Glass TabBar:**
    - On iOS 26, TabView gets automatic Liquid Glass styling. No manual glass needed on TabBar.
    - For iOS 16-25 fallback: the default TabView appearance is already appropriate. No changes needed.
    - Verify the RecordingView overlay still covers the TabBar correctly
    - Apply `.tint(Color.dictusAccent)` to TabView for the selected tab color
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && xcodebuild -project Dictus.xcodeproj -scheme DictusApp -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>
    RecordingView uses BrandWaveform instead of WaveformView. Old WaveformView struct removed. All main app screens use DictusColors + DictusTypography. Glass modifier applied to card surfaces. No hardcoded colors remain in modified files. Light and dark mode correct.
  </done>
</task>

<task type="auto">
  <name>Task 2: Keyboard extension — glass, brand waveform, animated mic button</name>
  <files>
    DictusKeyboard/Views/RecordingOverlay.swift,
    DictusKeyboard/Views/ToolbarView.swift,
    DictusKeyboard/Views/KeyboardView.swift,
    DictusKeyboard/Views/KeyButton.swift,
    DictusKeyboard/Views/FullAccessBanner.swift,
    DictusKeyboard/KeyboardRootView.swift
  </files>
  <action>
    **IMPORTANT — Keyboard extension cannot import DictusApp code.** The Design/ components (GlassModifier, BrandWaveform, AnimatedMicButton, DictusColors, DictusTypography) live in DictusApp target. For the keyboard extension, **copy these files into the DictusKeyboard target**:
    - Create `DictusKeyboard/Design/` directory
    - Copy GlassModifier.swift, BrandWaveform.swift, AnimatedMicButton.swift, DictusColors.swift, DictusTypography.swift into it
    - Register all 5 files in pbxproj under DictusKeyboard target (NOT DictusApp target)
    - If any file imports DictusApp-only modules, remove those imports (they should only need SwiftUI + DictusCore)

    **RecordingOverlay.swift — Brand waveform redesign:**
    - Replace the 30-bar KeyboardWaveformView with BrandWaveform
    - Compute a single energy Float from the `[Float]` waveformEnergy array (average or max of recent values)
    - BrandWaveform(energy: computedEnergy, maxHeight: 60) — smaller for keyboard context
    - Remove the old `KeyboardWaveformView` private struct entirely
    - Keep the transparent Color.clear background (native keyboard chrome shows through)
    - Timer and status text: use `.dictusBody` / `.dictusCaption` fonts
    - Cancel/stop icons: use semantic foreground colors (colorScheme-adaptive, as currently done)

    **ToolbarView.swift — AnimatedMicButton integration:**
    - Replace the inline `micButton` computed property and `micIcon()` helper with `AnimatedMicButton(status: dictationStatus, onTap: onMicTap)`
    - Remove the `@State private var pulsingOpacity` — animation is now in AnimatedMicButton
    - Remove the `micIcon(color:background:)` helper function
    - Keep the gear icon Link on the left side
    - For the disabled state (no Full Access): show AnimatedMicButton with `.idle` status but `.disabled(true)` and reduced opacity
    - Toolbar background: apply `.dictusGlassBar()` to the HStack background (replacing the plain separator line). Use `#available(iOS 26, *)` — on older iOS, keep the current separator approach.

    **KeyButton.swift — Glass key backgrounds:**
    - Current key buttons use a plain background color. Replace with `.dictusGlass(in: RoundedRectangle(cornerRadius: 5))` for each key.
    - CAVEAT from research: the native keyboard chrome on iOS 26 may already be glass. Wrap key glass in `#available(iOS 26, *)` — if the native chrome is already glass, adding glass to individual keys creates glass-on-glass artifacts. The GlassModifier already handles the fallback (Material on iOS 16-25).
    - Test approach: apply glass to key backgrounds. If it looks wrong on iOS 26 simulator (glass-on-glass), revert to the current styling for iOS 26 and only apply Material for iOS 16-25.
    - Use semantic foreground colors for key labels (`.primary` adapts to light/dark automatically)

    **KeyboardView.swift — Glass container:**
    - Apply `.dictusGlass()` to the overall keyboard container background IF it improves the appearance. Same caveat as KeyButton — native iOS 26 keyboard may already have glass. Test on simulator.
    - Use `.dictusGlassBar()` on any toolbar-like separators

    **FullAccessBanner.swift — Glass styling:**
    - This file already uses Material (it's the only file that does). Update to use `.dictusGlass()` modifier for consistency.
    - Ensure text uses `.dictusBody` and `.dictusCaption`
    - Settings deep-link button: use Color.dictusAccent

    **KeyboardRootView.swift — Typography pass:**
    - Verify all text uses system font styles (no fixed sizes)
    - No glass changes needed here (it's a container, not a surface)

    **Dynamic Type audit across all keyboard files:**
    - Search for `.font(.system(size:` in all keyboard files — replace fixed sizes with text style equivalents where possible
    - Timer in RecordingOverlay: keep monospaced `.system(size: 20, ...)` — timers need fixed width. Use `@ScaledMetric` to make the size scale with Dynamic Type
    - Icon sizes (16pt, 28pt in buttons): use `@ScaledMetric` for scaling
    - Ensure no text truncation at large accessibility sizes

    **Register all new DictusKeyboard/Design/ files** in pbxproj.
  </action>
  <verify>
    <automated>cd /Users/pierreviviere/dev/dictus && xcodebuild -project Dictus.xcodeproj -scheme DictusKeyboard -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1 | tail -5</automated>
  </verify>
  <done>
    Keyboard extension builds with: BrandWaveform replacing 30-bar waveform in RecordingOverlay. AnimatedMicButton replacing inline mic icon in ToolbarView. Glass modifier applied to keys, toolbar, and FullAccessBanner. Design/ files copied into DictusKeyboard target. Dynamic Type scaling via @ScaledMetric on icon/timer sizes. No hardcoded colors.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of design system pass</name>
  <action>Human verifies all visual changes on device/simulator</action>
  <what-built>
    Complete design system pass across all app and keyboard screens:
    1. 3-bar BrandWaveform on RecordingView (app) and RecordingOverlay (keyboard)
    2. AnimatedMicButton with glow/pulse/shimmer on keyboard toolbar
    3. Glass effect on cards, toolbars, keys, banners
    4. Brand colors throughout — no hardcoded values
    5. Dynamic Type typography — SF Pro Rounded headings, SF Pro Text body
    6. Light/dark mode support on all screens
  </what-built>
  <how-to-verify>
    1. Launch DictusApp on device/simulator. Verify Home tab shows glass cards, branded colors, Dynamic Type fonts.
    2. Navigate to Models tab — verify glass on model cards, accent colors on download buttons.
    3. Navigate to Settings tab — verify grouped list styling, proper typography.
    4. Trigger a dictation (dictus://dictate) — verify RecordingView shows 3-bar BrandWaveform, branded colors, glass result card.
    5. Switch to keyboard in any app — verify:
       a. Mic button in toolbar shows blue glow ring animation (idle state)
       b. Tap mic — recording overlay shows 3-bar waveform animating with audio
       c. Keys have glass/material styling appropriate for the iOS version
       d. FullAccessBanner (if shown) uses glass styling
    6. Toggle dark mode in Settings > Display — verify all screens adapt correctly with no hardcoded colors visible.
    7. Accessibility Inspector: set Dynamic Type to largest size — verify no text truncation, layout remains usable.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
1. `xcodebuild -project Dictus.xcodeproj -scheme DictusApp build` — succeeds
2. `xcodebuild -project Dictus.xcodeproj -scheme DictusKeyboard build` — succeeds
3. `cd DictusCore && swift test` — all tests pass (no changes to DictusCore in this plan)
4. Visual verification on device: glass, waveform, mic animation, dark mode, Dynamic Type
</verification>

<success_criteria>
- BrandWaveform replaces all old waveform views (50-bar in app, 30-bar in keyboard)
- AnimatedMicButton wired into keyboard ToolbarView with all 4 animation states
- Glass modifier applied to: app cards, keyboard keys, toolbar, FullAccessBanner
- No hardcoded Color literals remain in modified files — all use DictusColors or semantic colors
- All text uses system font styles (.dictusHeading/.dictusBody or system text styles)
- @ScaledMetric used for icon sizes and spacing in keyboard extension
- Light and dark mode render correctly on all screens
- Human verification passed on device
</success_criteria>

<output>
After completion, create `.planning/phases/04-main-app-onboarding-and-polish/04-03-SUMMARY.md`
</output>
