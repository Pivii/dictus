---
status: diagnosed
phase: 15.5-uikit-keyboard-rebuild-issue-59
source: [15.5-01-SUMMARY.md, 15.5-02-SUMMARY.md]
started: 2026-03-25T18:10:00Z
updated: 2026-03-25T19:15:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Type Letters
expected: Open any app with a text field. Tap letter keys on the Dictus keyboard. Each tap inserts the correct character into the text field.
result: pass

### 2. Zero Dead Zones
expected: Tap deliberately in the gaps between letter keys (the thin spaces separating keys). Every tap should register as the nearest key — no missed taps or unresponsive areas anywhere on the keyboard.
result: issue
reported: "il y a encore des dead zone horizontales et verticales"
severity: major

### 3. Key Popup Preview
expected: When tapping a letter key, a magnified popup preview of that letter appears above the key momentarily.
result: issue
reported: "Pop-ups première ligne coupés par le haut du clavier. Popup trop haute et pas de continuité visuelle entre la touche et la popup (pas de stem/neck comme Apple ou concurrents). La popup 'vole' au dessus de la touche."
severity: major

### 4. Accent Long-Press
expected: Long-press on a letter that has accents (e.g., "e", "a", "o"). An accent strip appears showing available accented variants. Sliding to one and releasing inserts it.
result: issue
reported: "Même souci que popups (première ligne coupée, pas de connexion touche-popup). En plus la touche A en haut à gauche: on ne voit pas toutes les propositions car elles débordent du clavier. Il faudrait que la popup soit décalée vers le centre."
severity: major

### 5. Delete Key
expected: Tapping delete removes the last character. Long-pressing delete continuously removes characters (repeat delete).
result: pass

### 6. Space Bar
expected: Tapping space inserts a space. If autocorrect has a suggestion, space triggers the correction.
result: pass

### 7. Return Key
expected: Tapping return inserts a newline (or performs the action indicated — "Send", "Search", etc. depending on context).
result: issue
reported: "Return key ne s'adapte pas au contexte (Search, Send, etc.). Aussi bug visuel: 4 touches spéciales (shift, delete, emoji, return) apparaissent noires/invisibles sans icône ni label visible."
severity: major

### 8. Shift and Caps Lock
expected: Single tap on shift: next letter is uppercase, then reverts to lowercase. Double tap on shift: caps lock activates (indicator changes), all letters uppercase until tapped again.
result: pass

### 9. Number/Symbol Layer Switch
expected: Tapping the "123" button switches to numbers/symbols layer. Tapping "ABC" switches back to letters. A second symbol page (#+= or similar) is accessible from the numbers layer.
result: issue
reported: "Il ne se passe rien à part le retour haptique"
severity: blocker

### 10. Emoji Key
expected: Tapping the emoji key opens the emoji picker view.
result: issue
reported: "Emoji picker s'ouvre mais layout clippé: titre catégorie (SMILEYS) coupé en haut à gauche, première colonne d'emojis tronquée à gauche, barre du bas (ABC, search, delete) aussi clippée des deux côtés."
severity: major

### 11. Trackpad Cursor Mode
expected: Long-press on the space bar activates trackpad/cursor mode. Dragging moves the text cursor in the text field.
result: pass

### 12. Globe Key
expected: Tapping the globe key switches to the next keyboard (system keyboard or another installed keyboard).
result: pass

## Summary

total: 12
passed: 6
issues: 6
pending: 0
skipped: 0

## Gaps

- truth: "Every pixel between keys resolves to a tap — zero dead zones"
  status: failed
  reason: "User reported: il y a encore des dead zone horizontales et verticales"
  severity: major
  test: 2
  root_cause: "UIStackView swallows unclaimed touches instead of returning nil. Over-constrained Auto Layout creates sub-pixel gaps. rowSidePadding creates 4pt edge dead zones. No hitTest override on container to route touches to nearest button."
  artifacts:
    - path: "DictusKeyboard/Views/UIKit/KeyboardContainerView.swift"
      issue: "No hitTest override; rowSidePadding creates edge dead zones; over-constrained width constraints"
    - path: "DictusKeyboard/Views/UIKit/LetterKeyButton.swift"
      issue: "point(inside:with:) expansion depends on parent forwarding touch"
  missing:
    - "Override hitTest on KeyboardContainerView to route touches to nearest button (bypass UIStackView chain)"
    - "Remove rowSidePadding or extend edge button touchInsets to cover it"
    - "Keep skip-last-button width constraint fix"
  debug_session: ".planning/debug/dead-zones-uikit-keyboard.md"

- truth: "Key popup shows magnified letter with visual stem connecting to the key"
  status: failed
  reason: "User reported: popup première ligne coupée, popup trop haute, pas de stem/neck entre touche et popup"
  severity: major
  test: 3
  root_cause: "KeyPopup draws plain RoundedRectangle with no stem shape. ZStack with .frame(height: keyboardHeight) clips first-row popups. Hardcoded -36 Y offset positions popup too high with no visual connection."
  artifacts:
    - path: "DictusKeyboard/Views/KeyboardTypes.swift"
      issue: "KeyPopup (lines 105-122) has no stem/neck shape"
    - path: "DictusKeyboard/KeyboardRootView.swift"
      issue: ".frame(height: keyboardHeight) clips popup (line 308); hardcoded -36 Y offset (line 314)"
  missing:
    - "Create custom KeyPopupShape Path with bubble + tapered stem"
    - "Move popup overlay outside height-constrained ZStack to avoid clipping"
    - "Compute Y offset dynamically from popup geometry (stemHeight + bubbleHeight/2)"
  debug_session: ".planning/debug/key-popup-preview-issues.md"

- truth: "Accent strip appears fully visible, repositioned for edge keys"
  status: failed
  reason: "User reported: même problème popup + touche A déborde du clavier, propositions non visibles"
  severity: major
  test: 4
  root_cause: "Accent popup always centered on key midX with zero edge clamping. For A key (midX ~20pt) with 7 accents (~252pt wide), strip starts at x=-106pt off the left edge. No keyboardWidth info available for clamping."
  artifacts:
    - path: "DictusKeyboard/KeyboardRootView.swift"
      issue: "Line 324: .position(x: accentKeyFrame.midX) with no clamping"
    - path: "DictusKeyboard/Views/UIKit/LetterKeyButton.swift"
      issue: "Line 185: popupStartX assumes centered positioning for touch hit-testing"
    - path: "DictusKeyboard/Views/UIKit/KeyboardTouchState.swift"
      issue: "No keyboardWidth property for edge clamping"
  missing:
    - "Add keyboardWidth to KeyboardTouchState"
    - "Clamp accent X: max(halfWidth, min(midX, keyboardWidth - halfWidth))"
    - "Match clamped offset in LetterKeyButton touch hit-testing"
  debug_session: ".planning/debug/accent-strip-edge-overflow.md"

- truth: "Return key adapts label to context and special keys show visible icons"
  status: failed
  reason: "User reported: return ne s'adapte pas au contexte + 4 touches spéciales (shift, delete, emoji, return) apparaissent noires sans icône"
  severity: major
  test: 7
  root_cause: "Two issues: (1) SF Symbol images set via setImage() are rendered behind the opaque backgroundView — backgroundView occludes UIButton's internal imageView. All title-based keys work, all image-based keys are invisible. (2) ReturnKeyButton hardcodes return.left SF Symbol with no mechanism to read textDocumentProxy.returnKeyType."
  artifacts:
    - path: "DictusKeyboard/Views/UIKit/SpecialKeyButtons.swift"
      issue: "BaseSpecialKeyButton.commonInit lines 47-65: backgroundView occludes imageView; ReturnKeyButton.setupIcon lines 474-476: hardcoded icon"
    - path: "DictusKeyboard/KeyboardRootView.swift"
      issue: "Never reads textDocumentProxy.returnKeyType"
  missing:
    - "After setImage in setupIcon(), call bringSubviewToFront(imageView) on each SF Symbol button"
    - "Read returnKeyType from textDocumentProxy and pass through KeyboardActions"
    - "Add updateReturnKeyType() method on ReturnKeyButton mapping types to labels"
  debug_session: ".planning/debug/special-key-issues.md"

- truth: "123 button switches to numbers/symbols layer"
  status: failed
  reason: "User reported: il ne se passe rien à part le retour haptique"
  severity: blocker
  test: 9
  root_cause: "KeyboardUIView.Coordinator change detection compares rows.count and rows.first?.count — all 4 layouts (AZERTY, QWERTY, numbers, symbols) have identical dimensions (4 rows, 10 first-row keys). Coordinator always concludes nothing changed and skips buildKeys() rebuild."
  artifacts:
    - path: "DictusKeyboard/Views/UIKit/KeyboardUIView.swift"
      issue: "Lines 49-57: Coordinator guard check compares row counts, never detects layer switch"
    - path: "DictusKeyboard/KeyboardRootView.swift"
      issue: "toggleLettersNumbers() correctly changes currentLayer but UIKit never rebuilds"
  missing:
    - "Pass currentLayer as property on KeyboardUIView and compare it in Coordinator"
    - "Or compare key definition identifiers instead of just counts"
  debug_session: ".planning/debug/special-key-issues.md"

- truth: "Emoji picker layout fills keyboard area without clipping"
  status: failed
  reason: "User reported: layout clippé - titre coupé, première colonne tronquée, barre du bas clippée"
  severity: major
  test: 10
  root_cause: "UIHostingController respects safe area insets from UIInputView parent. EmojiPickerView has minimal padding (2-10pt) and assumes full width. Normal UIKit keyboard handles this via KeyMetrics.rowSidePadding Auto Layout constraints, but EmojiPickerView bypasses KeyboardContainerView entirely."
  artifacts:
    - path: "DictusKeyboard/KeyboardRootView.swift"
      issue: "Lines 192-207: EmojiPickerView placed without padding or safe area handling"
    - path: "DictusKeyboard/Views/EmojiPickerView.swift"
      issue: "Lines 82-97: minimal edge padding"
    - path: "DictusKeyboard/Views/EmojiCategoryBar.swift"
      issue: "Lines 17-75: zero outer horizontal padding"
  missing:
    - "Add .padding(.horizontal, KeyMetrics.rowSidePadding) to EmojiPickerView in KeyboardRootView"
    - "Or add padding inside EmojiPickerView body VStack"
  debug_session: ".planning/debug/emoji-picker-clipping.md"

- truth: "Delete haptic feedback stops when text field is empty"
  status: failed
  reason: "User reported: retours haptiques continuent quand on laisse appuyé sur suppr et qu'il n'y a plus rien à supprimer"
  severity: minor
  test: 5
  root_cause: "DeleteKeyButton repeat loop (lines 230-244 SpecialKeyButtons.swift) fires haptic unconditionally. onDelete callback calls deleteBackward() which silently does nothing on empty field, but haptic fires before/independently of actual delete result."
  artifacts:
    - path: "DictusKeyboard/Views/UIKit/SpecialKeyButtons.swift"
      issue: "Lines 230-244: while loop fires haptic without checking if text was deleted"
    - path: "DictusKeyboard/KeyboardRootView.swift"
      issue: "onDelete closure doesn't return whether text was actually deleted"
  missing:
    - "Change onDelete to return Bool (true if text was deleted)"
    - "Check documentContextBeforeInput before deleting; return false if empty"
    - "Break repeat loop or skip haptic when onDelete returns false"
  debug_session: ""
