---
status: complete
phase: 15.4-zero-dead-zones
source: 15.4-01-SUMMARY.md, 15.4-02-PLAN.md (commits e22b8e9, cb3b530)
started: 2026-03-25T08:00:00Z
updated: 2026-03-25T13:35:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Version/build in keyboard launch log
expected: Open Settings > Dictus > Debug Logs. The first log entry after keyboard launch shows version=X.X and build=X as the first fields.
result: pass
verified: Log file confirms `version=1.2 build=1` in KBD viewDidLoad entry

### 2. Normal typing (no regression)
expected: Type "bonjour le monde" using the keyboard. All letters produce the correct character with audio and haptic feedback. No missed keystrokes.
result: skipped
reason: Phase closed early — dead zone approach pivoting to UIKit rebuild (phase 15.5)

### 3. Shift, delete, space, return keys
expected: Shift produces uppercase on next letter. Delete removes characters. Space inserts space. Return inserts newline. All with audio/haptic.
result: skipped
reason: Phase closed early — dead zone approach pivoting to UIKit rebuild (phase 15.5)

### 4. Long-press behaviors (no regression)
expected: Long-press 'e' shows accent popup. Long-press delete repeats deletion. Long-press space activates trackpad mode.
result: skipped
reason: Phase closed early — dead zone approach pivoting to UIKit rebuild (phase 15.5)

### 5. Horizontal gap taps (letter rows)
expected: Tap between letter keys (Q-W, G-H, O-P, A-S, K-L, Z-X, N-M). Each gap tap resolves to the nearest key with audio + haptic. No silent dead zones.
result: issue
reported: "SwiftUI cannot resolve dead zones — per-key DragGesture takes priority over catch-all gesture, leaving gaps between keys unresponsive"
severity: blocker

### 6. Left/right margin taps
expected: Tap left of Q, left of A, left of Z (left margins) and right of P, right of L, right of M (right margins). Each resolves to the nearest edge key with audio + haptic.
result: skipped
reason: Phase closed early — blocked by SwiftUI gesture limitation

### 7. Row boundary taps (vertical gaps)
expected: Tap in the vertical gap between row 1 and row 2, between row 2 and row 3, and between row 3 and the bottom row. Each resolves to a key in the nearest row.
result: skipped
reason: Phase closed early — blocked by SwiftUI gesture limitation

### 8. Corner taps
expected: Tap top-left corner (above Q), top-right corner (above P), bottom-left corner (below space area), bottom-right corner (below return). All resolve to the nearest key.
result: skipped
reason: Phase closed early — blocked by SwiftUI gesture limitation

### 9. Numbers/symbols layer gaps
expected: Switch to 123 and #+= layers. Tap a few gaps between keys. They resolve correctly with audio + haptic, no dead zones.
result: skipped
reason: Phase closed early — blocked by SwiftUI gesture limitation

### 10. Temporary logging removed
expected: No "gap-tap" or "TEMPORARY" entries appear in the keyboard code. Version/build logging is still present. This is a code-level check — Claude will verify.
result: pass
verified: grep confirms no "gap-tap" or "TEMPORARY" in KeyboardView.swift, version/build logging present in KeyboardViewController.swift

## Summary

total: 10
passed: 2
issues: 1
pending: 0
skipped: 7

## Gaps

- truth: "Every pixel in the keyboard surface resolves to a key when tapped"
  status: failed
  reason: "SwiftUI cannot resolve dead zones — per-key DragGesture takes priority over catch-all gesture. The problem was diagnosed, the solution identified (UIKit rebuild), and work transferred to phase 15.5 / issue #59."
  severity: blocker
  test: 5
  root_cause: "SwiftUI gesture system limitation — per-key DragGesture consumes touches in gap areas, preventing the catch-all background gesture from firing"
  artifacts:
    - path: "DictusKeyboard/Views/KeyboardView.swift"
      issue: "handleDeadZoneTap catch-all gesture never fires in gaps because SwiftUI routes gap touches to adjacent key DragGestures"
  missing:
    - "UIKit-based keyboard view with unified touch handling (phase 15.5)"
  debug_session: "issue #56, issue #59"
